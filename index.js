document.addEventListener("DOMContentLoaded", () => {
  let typed = new Typed(".typing", {
    strings: ["Student", "Developer"],
    typeSpeed: 100,
    backSpeed: 50,
    loop: true,
    loopCount: Infinity,
  });
});
